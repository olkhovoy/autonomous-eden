"""
UMC Benchmark Tests

This package contains specialized tests for evaluating unique capabilities
of the recursive fixed-point transformer architecture:

- adaptive_depth: Tests if model uses different iteration counts for different inputs
- online_learning: Tests rapid adaptation capability during inference
- self_reference: Tests model's ability to reason about its own computation
"""
