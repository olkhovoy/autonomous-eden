{
  "model_type": "both",
  "dataset_name": "openwebtext",
  "dataset_subset": null,
  "max_train_samples": 500000,
  "max_eval_samples": 5000,
  "max_steps": 50000,
  "batch_size": 4,
  "learning_rate": 3e-4,
  "warmup_steps": 1000,
  "gradient_accumulation_steps": 8,
  "max_seq_length": 512,
  "eval_steps": 2500,
  "eval_lambada": true,
  "eval_hellaswag": false,
  "save_steps": 5000,
  "output_dir": "benchmark_output",
  "device": "cuda",
  "use_amp": true,
  "num_workers": 2,
  "use_wandb": false,
  "wandb_project": "umc-benchmark",
  "_comment": "RTX 3090 24GB optimized configuration. Effective batch size = 4 * 8 = 32"
}
