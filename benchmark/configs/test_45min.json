{
  "_comment": "45-minute test configuration for RTX 3090 24GB. Calculation: ~25k tokens/sec * 60 sec / 16384 tokens/step = ~90 steps/min. 45 min = ~4000 steps.",
  
  "model_type": "both",
  "dataset_name": "openwebtext",
  "dataset_subset": null,
  
  "max_train_samples": 100000,
  "max_eval_samples": 5000,
  
  "batch_size": 4,
  "gradient_accumulation_steps": 8,
  "max_seq_length": 512,
  
  "learning_rate": 3e-4,
  "warmup_steps": 400,
  "max_steps": 4000,
  
  "eval_steps": 500,
  "save_steps": 1000,
  "log_steps": 50,
  
  "eval_lambada": true,
  "eval_hellaswag": false,
  
  "output_dir": "benchmark_output",
  "device": "cuda",
  "use_amp": true,
  "num_workers": 2,
  
  "use_wandb": true,
  "wandb_project": "umc-benchmark"
}
